import React from 'react'

const Practice = (props) => {
  return (
    <div>
      <h4>{props.fName}</h4>
      <h4>{props.lName}</h4>
      <h4>{props.age}</h4>
    </div>
  )
}
export default Practice
